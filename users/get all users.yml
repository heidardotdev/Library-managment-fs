info:
  name: get all users
  type: http
  seq: 3

http:
  method: GET
  url: localhost:19000/api/users
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
