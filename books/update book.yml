info:
  name: update book
  type: http
  seq: 4

http:
  method: PUT
  url: localhost:19000/api/books?id=1
  params:
    - name: id
      value: "1"
      type: query
  body:
    type: json
    data: |-
      {
        "title": "oprating systems",
        "author": "Abraham Silbershotz",
        "price": 1800000
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
